# This portfolio does not require application secrets for its default Netlify setup.
# Netlify automatically provides the site context required by @netlify/blobs.
#
# If you later add an external email/database provider, put its server-side
# credentials here and never commit the real .env file.

# Firebase Web config is stored in firebase-config.js (public web config only).
